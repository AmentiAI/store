export const metadata = { title: "Returns" };

export default function ReturnsPage() {
  return (
    <div className="mx-auto max-w-2xl px-4 py-16 sm:px-6 lg:px-10">
      <h1 className="font-[family-name:var(--font-display)] text-4xl tracking-tight sm:text-5xl">
        Returns
      </h1>
      <div className="mt-8 space-y-4 text-sm leading-relaxed text-muted">
        <p>
          14-day hassle-free returns on eligible items in original condition.
        </p>
        <p>
          Start a return from your order confirmation email or contact support.
        </p>
        <p>
          Refunds are issued to the original payment method once we inspect the
          return.
        </p>
      </div>
    </div>
  );
}
